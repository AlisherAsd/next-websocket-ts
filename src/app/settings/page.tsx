import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Settings",
}

/**
 * Страница настроек
*/
export default function SettingsPage() {
    return <div>
        Settings Page
    </div>
}